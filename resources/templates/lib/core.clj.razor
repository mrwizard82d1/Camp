@inherits Westwind.RazorHosting.RazorTemplateBase
(ns @string.Format("{0}.core", Model["name"]))

(defn foo 
    "What I do is up to you."
    [& args]
    (println "TODO: @Model["name"] all the things."))
